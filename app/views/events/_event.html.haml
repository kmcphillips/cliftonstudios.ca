%h1password"],
        :database => config["database"]
    )
    puts "Connected"
  end

  def import!
    begin
      ActiveRecord::Base.transaction do
        clear_tables
        import_members
        import_posts
        import_events
        import_about
        import_images

        puts "Deleting pending emails"
        ActiveRecord::Base.connection.execute("TRUNCATE pending_emails")

        puts "Success!"

  - if event.new_record?
    = event.title
  - else
    = link_to event.title, event_path(event)
    
.subtext_left
  = event.prefix
  = event.starts_at.to_s :with_time_no_minutes
.subtext_right
  By
  = link_to event.member.name, artist_path(event.member)

.post.clear_right
  = link_to image_tag(event.inline, :class => "inline", :alt => event.title), event.full, :rel => "lightbox", :class => "thumb inline" if event.image.exists?
  = auto_link(simple_format(event.body)).html_safe
  .clear_right

