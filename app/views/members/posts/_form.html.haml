= form_for @post do |f|
  = f.error_messages

  %ul.form
    = f.item :text_field, :title, :size => 40
    = f.item :text_area, :body, :cols => 30, :rows => 8
    - if f.object.permalink
      %li
        = f.label :permalink
        = f.object.permalink

    = f.item :submit, "Save"

