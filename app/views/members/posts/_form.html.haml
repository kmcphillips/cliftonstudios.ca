= form_for [:members, @post] do |f|
  = f.error_messages

  %ul.form
    = f.item :text_field, :title, :size => 50
    = f.item :text_area, :body, :cols => 50, :rows => 12
    - if f.object.permalink
      %li
        = f.label :permalink
        = f.object.permalink

    = f.item :submit, "Save"

