= form_for @link do |f|
  = f.error_messages

  %ul.form
    = f.item :text_field, :title, :size => 40
    = f.item :text_field, :url, :size => 40
    = f.item :text_area, :description, :cols => 30, :rows => 4
    = f.item :submit, "Save"

