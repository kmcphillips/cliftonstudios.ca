%h1
  Editing:
  = @member.name

.post
  = render :partial => "form"

  .image_links
    = index_entity_image members_members_path, "Back to Studio Members Index"
    %br
    = show_entity_image members_member_path(@member), "Show #{@member.name}'s Profile"
    %br
    = show_entity_image artist_path(@member), "View #{@member.name} on the public site"