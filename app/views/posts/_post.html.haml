%h1
  - if post.new_record?
    = post.title
  - else
    = link_to post.title, post_path(post)
    
.subtext_left= post.created_at.try(:to_s, :with_time_no_minutes)
.subtext_right
  By
  = link_to post.member.name, artist_path(post.member)

.post.clear_right
  = simple_format post.body
