%h1 About Clifton Studios

.post
  = link_to image_tag(@about.inline, :class => "inline", :alt => "Clifton Studios"), @about.full, :rel => "lightbox", :class => "thumb inline" if @about.image.exists?
  = simple_format @about.body
  .clear_right

%h1 Contact

.post
  Clifton Studios is located half a block off Portage Avenue, just past Erin and Wall.
  %br
  %br
  = render :partial => "shared/address"
  %br
  = obfuscated_mail_to ADMIN_EMAIL
  %br
  %br
  = render :partial => 'map'

%h1 How To Join

.post= simple_format @availability.body

- if logged_in?
  %h1 Studio Bylaws
  .post
    = link_to "View studio bylaws", members_bylaws_path
