%h2 Site Search
= form_tag search_path do
  %p
    = text_field_tag :search, nil, :size => 16
    = submit_tag "Search", :style => "margin-top: 10px"


- if (events =  Event.upcoming).any?
  %h2 Upcoming Events
  %ul
    - events.each do |event|
      %li
        = link_to event.title, event_path(event)
        %br
        = event.starts_at.to_s(:with_time)


%h2 Follow Us
%ul
  %li.social
    %table
      %tr
        %td= social_media_icon :facebook, "http://www.facebook.com/pages/Clifton-Studios/111063175627791"
        %td= social_media_icon :rss, "/rss.xml", :alt => "Follow our RSS feed"


%h2 Mailing List
= form_tag mailing_list_index_path do
  %p Sign up for our mailing list to be updated on shows and events.
  %p
    = text_field_tag :email, nil, :size => 16
    = submit_tag "Sign Up", :style => "margin-top: 10px"
    = text_field_tag :validation, nil, :size => 1, :style => "display: none"
